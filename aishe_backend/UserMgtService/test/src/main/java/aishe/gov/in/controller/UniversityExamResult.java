package aishe.gov.in.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class UniversityExamResult {
	/*
	 * private static final Logger logger =
	 * LoggerFactory.getLogger(University.class);
	 * 
	 * @Autowired UniversityExamResultService universityExamResultService;
	 * 
	 * @PostMapping("/saveOrupdateUniversityExamResult") public
	 * ResponseEntity<ReturnResponse> saveOrUpdateUniversityExamResult(
	 * 
	 * @RequestBody ExaminationResultEO examinationResultEO) { logger.
	 * info("university Exam Result controller : saveOrUpdateUniversityExamResult method invoked : {}"
	 * , examinationResultEO); boolean isSaved =
	 * universityExamResultService.saveOrUpdateUniversityExamResult(
	 * examinationResultEO); if (isSaved) { return new ResponseEntity<>( new
	 * ReturnResponse(HttpStatus.OK.value(),
	 * "Exam Result Details has been  Successfully Saved!!"), HttpStatus.OK); } else
	 * { return new ResponseEntity<>(new
	 * ReturnResponse(HttpStatus.BAD_REQUEST.value(),
	 * "Request Cannot Be Processed. Please Try Again."), HttpStatus.BAD_REQUEST); }
	 * 
	 * }
	 * 
	 */}
