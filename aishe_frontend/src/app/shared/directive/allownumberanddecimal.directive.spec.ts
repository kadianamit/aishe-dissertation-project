import { AllownumberanddecimalDirective } from './allownumberanddecimal.directive';

describe('AllownumberanddecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new AllownumberanddecimalDirective();
    expect(directive).toBeTruthy();
  });
});
